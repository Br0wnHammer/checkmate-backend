name: Staging Deploy

on:
    push:
        # branches:
        #     - develop

jobs:
    deploy:
        runs-on: self-hosted

        steps:
        - name: Checkout code
          uses: actions/checkout@v2

        - name: Build Docker image
          run: |
            ls -l docker/prod
            docker build -t uptime-backend:develop ./docker/prod/server.Dockerfile
